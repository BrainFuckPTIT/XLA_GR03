# Kết quả

Chỉ commit kết quả gọn cần để tái hiện kết luận:

- CSV/JSON metric đã tổng hợp;
- biểu đồ cuối;
- một số ảnh minh họa đại diện.

Prediction cho toàn dataset và output tạm đặt trong `results/predictions/`
hoặc `results/tmp/`; hai thư mục này bị bỏ qua bởi Git.
